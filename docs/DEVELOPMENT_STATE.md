# AgentBridge Development State

Last updated: 2026-06-27

## Product Understanding

AgentBridge is a local-first collaboration control layer for native programming-agent CLIs. The product must preserve a real terminal session on the user's machine, expose bot-friendly semantic events, and enforce a single-writer lease so group chat users, a web admin surface, and the local human cannot corrupt the same interactive CLI state.

The authoritative design input is `AgentBridge_项目总设计文档_v0.2_多项目多会话指令版.md`.

## Implementation Status

Current milestone: M0 backend foundation.

Current direction (2026-06-27): dropped the one-shot `codex exec` route in favor of the
designed **persistent interactive session** model, with Claude and Codex both wired as
first-class agents and a humane command layer for switching agents / sessions / projects.
The unified execution model is **native TUI in a PTY** for both agents (so human takeover is
identical), with Claude semantics from Claude Code Hooks and Codex turn-completion from a
PTY-idle heuristic.

Persistent execution wiring (this iteration, all unit-tested):

- Command layer (`commands.py`): `/agent status` unified status card (project / session / agent /
  control lease / queue / active turn / sibling sessions); `/agent claude|codex [task]` switches the
  active session to that agent, creating one if absent and queuing any trailing task; `/agent agents`
  grouped listing; grouped Chinese `/agent help`. Chinese aliases `状态/切换/使用` included.
- Persistent runner (`TerminalAgentService.advance_queue` / `advance_pending_queues`): when a session is
  idle, its terminal is running, the queue has work, and no human holds the lease, it claims the next
  queued Turn and submits it into the native TUI; on turn completion (semantic event → `finish_turn` →
  IDLE) it advances the next Turn. Opt-in via `AGENTBRIDGE_TERMINAL_AUTO_ADVANCE_QUEUES`, wired into the
  lifecycle monitor tick.
- Codex idle-completion (`check_idle_turn_completions`): for hook-less agents (Codex / generic TUI),
  tracks PTY output cursor and emits `turn.completed` after `AGENTBRIDGE_CODEX_IDLE_COMPLETE_SECONDS` of
  silence; Claude is excluded (it uses the Stop hook). Opt-in via `AGENTBRIDGE_TERMINAL_IDLE_TURN_COMPLETION`.
- Claude Hooks deployment (`claude_hook_deploy.py`): on a Claude session start, merges AgentBridge hooks
  into `<workspace>/.claude/settings.local.json` (idempotent, preserves unrelated settings) so the native
  `claude` TUI POSTs structured events back. Opt-in via `AGENTBRIDGE_CLAUDE_HOOK_DEPLOY` (+ API url/token).
- Bot plugin (`/Volumes/data/mybot/.../agentbridge_gateway.py`): rewritten to route every command
  (including `ask/send/continue`) through the AgentBridge command path into the persistent session, and to
  stream rendered events back to the group by polling `GET /sessions/{id}/rendered-events` (now includes
  `type`) until a terminal turn event. The one-shot `codex exec` path is removed from dispatch (original
  saved as `.codex-exec.bak`).

End-to-end live verification (group → streaming → human takeover → resume) still requires local `claude` /
`codex` CLIs and the server launched with the opt-in flags above plus the lifecycle monitor enabled.

### Delivery troubleshooting log (2026-06-27): queued replies showed 503 / never arrived

A live trial surfaced three independent defects in the bot→gateway delivery chain. Recorded here so
future delivery regressions can be diagnosed the same way (logs + the new `active` flag + bot stream logs).

1. **`503` on every queued message, and the first answer never came back.** Root cause was *transport*,
   not the queue: the QQ bot process inherits `HTTP_PROXY/HTTPS_PROXY=127.0.0.1:10808` (meant for the
   *agents'* outbound model API) and its `httpx` clients used the default `trust_env=True` with no
   `NO_PROXY`, so localhost gateway calls (`127.0.0.1:8000`) were routed *through* the forward proxy.
   Under the concurrent command-POST + background `chat-events` polling load the proxy intermittently
   returned a non-JSON `503`. Fix (bot repo): `request_agentbridge` — the single chokepoint for all
   gateway calls — now creates its client with `trust_env=False`. Server-side never returns `503` for
   queue/chat/onebot paths (only audit-signing / device-cert do), which is the tell that a `503` there
   is proxy-originated.
2. **Multi-turn queue: only the first queued turn's answer was delivered.** The bot decided "round over"
   from message `kind` (`answer`/`error`), so the first turn's answer ended the stream and abandoned the
   rest of the queue. Fix: `GET /sessions/{id}/chat-events` now returns an authoritative `active` flag
   (`active_turn_id` set, or queued turns remain) plus `active_turn_id` / `queued_turns` / `queue_paused`;
   the bot streams until `active` is `False` instead of guessing from message kind.
3. **"Sleep N then answer" (background command): the real answer never reached chat.** When Claude runs a
   `sleep` as a *background* command it prints "已开始休眠…", yields, and the Stop hook fires
   `turn.completed` *early*; the actual summary is written N seconds later as `assistant.delta` events with
   **no `turn_id` and no following `turn.completed`**. The old projection only emitted answers at
   completion, so this "orphan tail" was never gathered nor held → lost; and repeated Stops re-merged the
   growing tail into duplicate answers. Fixes (`chat_stream.py`): emit orphan-tail deltas (post-completion
   final-zone deltas of an already-finished turn) as standalone answers exactly once; make each
   `turn.completed` only coalesce deltas since the previous completion (`answered_upto`), killing the
   growing duplicates; and only show "✅ 本轮已完成。" for turns that produced no deltas at all. The bot
   also keeps polling for `AGENTBRIDGE_BOT_STREAM_IDLE_GRACE_SECONDS` (default 45s) after `active` goes
   `False` so a late background answer is still picked up. Regression tests live in `tests/test_chat_stream.py`
   (`test_orphan_tail_*`, `test_progress_only_turn_has_no_redundant_done_marker`).

All three were verified end-to-end against live Claude: two queued "summarize in N words, sleep 20s first"
tasks now deliver both summaries, in order, exactly once, with no `503` and no duplicate/`✅`-noise.

Implemented in this slice:

- Python/FastAPI project skeleton.
- In-memory Control Plane domain model and repository.
- Project, Workspace, Session, Turn, Interaction, WriterLease, ChatContext, CommandInvocation, and AuditEvent models.
- Structured command registry exported through `GET /api/v1/commands`, covering implemented `/agent` canonical commands with aliases, usage, argument JSON Schemas, required permission, target mode, risk, confirmation, private-result, and renderer metadata.
- `/agent` text command parsing and execution for:
  - `project list/info/use/create`
  - `session list/new/use/info/close`
  - `ask` / `send`
  - `control status/takeover/release`
  - `health`
- Text fallback command recovery now includes numbered project/session selection through `/agent select project <number>` and `/agent select session <number> [--project <project>]`, using the same visible list order as `project list` and `session list` and keeping session selection synchronized with the selected Session's owning project.
- `/agent session use <session>` and the localized shortcut `/agent 使用 <session>` resolve Session IDs or short codes across projects and synchronize the chat context's active project to the selected Session's owning project, matching the numbered selector behavior.
- Text-only Interaction handling now accepts one-based pending-list numbers anywhere an Interaction ID is accepted for question answers, approval show/cancel/vote, and plan show/approve/revise/cancel commands; numeric arguments are type-filtered against `/agent question list`, `/agent approvals`, or `/agent plan list`, so `/agent approve 1` cannot accidentally resolve a question or plan.
- Project, Session, and Interaction list command messages now render one-based numbered text rows and command-specific hints, so text-only users can discover the same selectors that `/agent select project|session`, `/agent answer`, `/agent approve`, `/agent deny`, and `/agent plan ...` accept without copying UUIDs.
- Missing-argument command errors now return command-specific recovery hints in `next_step`, including the relevant list command, numbered selector, queue version lookup, or draft-style command example for project/session selection, queue operations, questions, approvals, plans, roles, policies, and lease release.
- Chat-context project binding commands now cover `/agent project bind <project> [--alias <alias>] [--default]`, `/agent project bindings`, and `/agent project default <project>`; existing bindings can be promoted to the unique default project, chat aliases are preserved for project resolution, and SQLAlchemy recovery preserves binding/default state.
- Chat-space project bindings are readable through `GET /api/v1/chat-spaces/{chat_context_id}/project-bindings`, gated by managed-device `project_read` and chat-context resource allowlists, and the Project/Session Admin UI can display bindings for an operator-supplied chat context alongside project/workspace/session state.
- Idempotent command execution by idempotency key.
- Parsed command execution now writes command-level audit records for both successful execution (`command.executed`) and execution-stage failures (`command.failed`), preserving invocation ID, trace ID, canonical command, error code, status code, and actor; permission-denied command failures are audited with `denied` outcome, while other execution errors are audited as `failed` and are not stored in the idempotent command-result cache.
- Optimistic locking for active project/session pointers.
- Writer lease epoch handling with local human preemption over bot control.
- Turn enqueue now detects an active human writer lease and keeps the task queued with `queue_reason=human_control`, carrying lease owner/epoch metadata through the Turn record, audit details, `turn.queued` semantic event payload, `/agent ask` command result, and Bot text rendering so group users can see that work is waiting for local control to be released.
- Releasing a human writer lease now emits `turn.queue_unblocked` when human-control queued Turns exist, carrying the next Turn ID, unblocked count, queue version, pause state, and next epoch; Bot text rendering surfaces this as a resume notice that the same Session can continue after local control is released.
- Terminal Agent offline protection can be toggled through the Control Plane, REST `POST /api/v1/sessions/{id}/terminal/offline-protection`, Terminal WebSocket `set_offline_protection`, and local daemon `set_offline_protection`; enabling it moves the Session to `recovering`, revokes any non-Human writer lease while advancing the lease epoch, rejects new Bot/Web/Admin leases, leaves local Human lease takeover available, keeps Bot tasks queued with `queue_reason=terminal_agent_offline`, blocks queued-Turn claims until protection is disabled, emits audited `terminal.offline_protection_*` events, and renders operator/Bot notices for the protected queue state. Terminal lifecycle detection now automatically enables the same protection after `terminal.lost`, recovers the projection from persisted lost events if a process stops between event and state update, and disables protection after a successful `terminal.started` recovery.
- Terminal Agent or adapter executors can atomically claim the next queued Turn through `POST /api/v1/sessions/{id}/queue/claim-next`, Terminal WebSocket action `claim_next_turn`, or local daemon action `claim_next_turn`, guarded by `session.manage`, optional `expected_queue_version`, queue-pause checks, active-Turn serialization, and project running-Turn quota; successful claims mark the Turn running, update the Session active Turn, emit audited `turn.claimed` plus `turn.started`, render a Bot-visible start notice, and persist through SQLAlchemy snapshot recovery.
- Terminal WebSocket and local daemon `claim_next_turn` can optionally run with `submit_prompt=true`; the Terminal Agent verifies the native terminal is running, acquires or renews the requested writer lease (default `bot:terminal-agent`), claims the queue head, and submits the Turn prompt as terminal text with an idempotent input request ID.
- Project create APIs, the Project/Session Admin UI, and `/agent project create` expose `max_active_sessions`, `max_running_turns`, `max_queued_turns`, and `daily_turns_per_user`; Session and Turn lifecycle operations reject over-quota projects with `QUOTA_EXCEEDED` until inactive sessions, non-running/non-queued Turns, or the UTC daily user quota reset release capacity.
- Turn queue management APIs and commands now cover `queue list`, `queue remove`, `queue clear`, `queue move`, `queue pause`, and `queue resume`; maintainers can reorder or pause/resume queued Turns through optimistic `queue_version` checks, queue clearing requires confirming the affected Turn count, operators may remove their own queued Turns, and clearing or removing another actor's queued Turn requires session management permission.
- The Project/Session Admin UI shows active Turn, queued-Turn count/pause state, pending approval count, and current writer-lease ownership in the Session table, plus the selected Session queue with `queue_version`, pause state, queued Turn rows, and controls for refresh, pause, resume, and confirmed clear.
- Workspace create APIs and the Project/Session Admin UI expose `is_writable` and `max_write_sessions`; `read_only` workspaces are normalized to non-writable with zero write slots, and writer lease acquisition enforces the resulting capacity across sessions sharing a Workspace.
- Workspace registration resolves both `path` and `allowed_root` through real paths before enforcing the allowed-root boundary; symlinks that resolve inside the allowed root are accepted with normalized paths, while symlink escapes outside the allowed root are rejected with `WORKSPACE_PATH_DENIED`.
- Audit hash chain for command and domain state changes.
- Filtered audit API through `GET /api/v1/audit`, backed by repository-level newest-first queries bounded by `limit` and supporting action, actor, trace, project, session, interaction, payload text, and created-at time-window filters for operational review; the same filters can be exported through `GET /api/v1/audit/export` as JSON, CSV, an external command signed archive when `AGENTBRIDGE_AUDIT_ARCHIVE_SIGNING_COMMAND` is configured for KMS/HSM/Vault integration, an Ed25519/RSA-PSS/ECDSA signed archive when `AGENTBRIDGE_AUDIT_ARCHIVE_SIGNING_PRIVATE_KEY_FILE` is configured, or an HMAC-SHA256 signed archive when `AGENTBRIDGE_AUDIT_ARCHIVE_SIGNING_KEY(_FILE)` is configured; `agentbridge-audit-verify` provides offline HMAC/public-key archive verification, and SQLAlchemy uses indexed action, actor, trace, project, session, interaction, and created-at columns before payload-level filters.
- Ordered semantic event streams for project/session state changes.
- Control Plane can store per-session semantic-event consumer ACK offsets, keyed by consumer ID, with monotonic `last_seq` updates persisted through SQLAlchemy migration `0013_event_consumer_offsets`.
- REST event replay through `GET /api/v1/sessions/{id}/events`.
- Cross-stream semantic event search through `GET /api/v1/events`, backed by repository-level newest-first queries bounded by `limit` and supporting project, session, turn, interaction, event type, source, trace, payload text, and created-at time-window filters for operational investigation.
- Cross-stream rendered event search through `GET /api/v1/events/rendered`, using the same operational filters and returning render documents plus OneBot/plain-text messages for operator-facing notifications; filtered cross-stream events can also be delivered through Bot Gateway delivery records with `POST /api/v1/bot-gateway/deliver-events`.
- Audit and semantic event search support bounded `q`, dot-path field/value filters, `created_from`, and `created_to` filters, exposed through REST and the Audit & Events Admin Web page; SQLAlchemy stores normalized audit `details` and semantic event `payload` search text so `q` filters can run in the database before row hydration, uses JSON path expressions where they safely narrow exact field/value candidates, and always verifies dot-path matches after model hydration so string, numeric, boolean, and null scalar filters share the same semantics across repositories.
- Idempotent Terminal Agent event ingestion through `POST /api/v1/sessions/{id}/events`.
- Terminal Agent lifecycle events can use an explicit JSONL offline outbox through `AGENTBRIDGE_TERMINAL_EVENT_OUTBOX`; `terminal.started`, `terminal.exited`, `terminal.lost`, and skipped auto-restart notices are appended locally on transient Control Plane submission failures, then flushed in order before the next lifecycle event, each lifecycle monitor poll, `POST /api/v1/terminal/event-outbox/flush`, or local daemon `flush_event_outbox`, with stable idempotency keys preserving at-least-once delivery semantics and lifecycle status exposing outbox pending/read/last-flush state.
- Idempotent structured Agent Adapter event ingestion through `POST /api/v1/sessions/{id}/agent-adapter/events`, requiring the reported `agent_type` to match the Session `agent_type` and the declared `schema_version` to pass the compatibility matrix (`claude-hooks.v1` for Claude and `codex-app-server.v1` for Codex). Known Claude Hook and Codex app-server event names normalize into AgentBridge semantic events (`assistant.delta`, `tool.*`, `tool.output.delta`, `approval.requested`, `question.requested`, `diff.updated`, `plan.updated`, `turn.completed`, and `turn.failed`) while preserving the raw adapter payload under `raw_event`; adapter approval, question, and plan requests create pending Interaction records using the adapter event idempotency key when supplied, and adapter processes can poll `GET /api/v1/sessions/{id}/agent-adapter/responses` for answers, approval decisions, cancellations, and expirations tied to their adapter-originated interactions. The packaged `agentbridge-adapter-client` module/CLI now provides reusable Claude Hook and Codex app-server bridge clients for handshake JSON with schema snapshots, a provider-captured Claude Hook projection from the official Hook reference plus local `claude --version` evidence (`2.1.193 (Claude Code)`), and a provider-captured Codex schema projection from `codex app-server generate-json-schema` (`codex-cli 0.141.0`), schema-checked event submission, API-token or device-key authentication headers, response polling, `emit-and-wait` blocking waits that match responses by request event, interaction, or adapter item id, `schemas` inspection for the current per-version event/normalization/provider-coverage/response behavior matrix, `format-response` output for Claude Hook command stdout plus a stable Codex app-server action envelope and JSON-RPC `result.agentbridge` wrapper when a request id is known, a `claude-hook` command-hook shim that reads Claude stdin JSON, reports observer events, blocks on approval/question events, prints Claude stdout JSON, `claude-hooks-config` generation/merge for Claude Code settings using exec-form command hooks while preserving unrelated existing hooks, `codex-app-server-event` for one Codex app-server JSON-RPC message, `codex-app-server-stream` for newline-delimited app-server JSONL streams, and `codex-app-server-proxy` for launching a Codex app-server stdio subprocess while forwarding stdin/stdout through a single upstream stdin router, teeing child JSONL into AgentBridge, optionally emitting response side-channel JSONL, optionally injecting AgentBridge JSON-RPC interaction responses back into child stdin, optionally supervising child exits with a bounded restart policy plus unhealthy-exit summary counts, and optionally emitting proxy health JSONL for started/running/exited/restarting/stopped states.
- `agentbridge-adapter-client` can use an explicit JSONL offline outbox through `--offline-outbox` or `AGENTBRIDGE_ADAPTER_OFFLINE_OUTBOX`; schema-validated adapter events are appended on transient Control Plane submission failures and flushed in order before the next event after reconnect or through the explicit `flush-outbox` CLI command, while interaction waits fail closed when the request event is only queued locally.
- Session semantic event WebSocket stream through `/api/v1/sessions/{id}/events/ws`, with `after_seq` replay and live tailing.
- Bot-facing rendered event WebSocket stream through `/api/v1/sessions/{id}/rendered-events/ws`, returning render documents plus OneBot/plain-text messages.
- Optional `AGENTBRIDGE_API_TOKEN` or `AGENTBRIDGE_API_TOKEN_FILE` authentication for REST API routes other than `/api/v1/health`, accepting bearer tokens or `X-AgentBridge-API-Token`, with unlocked Admin Web cookies accepted when `AGENTBRIDGE_ADMIN_TOKEN`/`AGENTBRIDGE_ADMIN_TOKEN_FILE` is configured; if only the API token is configured, it also gates and unlocks the built-in Admin Web pages.
- Optional `AGENTBRIDGE_WS_TOKEN` or `AGENTBRIDGE_WS_TOKEN_FILE` authentication for session event, rendered event, and terminal command WebSocket routes; token files are reread per connection, and same-origin browser sessions with an unlocked Admin Web cookie can connect to protected WebSocket streams without exposing the token to page JavaScript.
- Optional `AGENTBRIDGE_DEVICE_KEYS` JSON mapping for per-device HTTP and WebSocket keys, using device ID plus device key rather than one shared bearer token.
- Database-managed device identities through `/api/v1/device-identities`, storing salted PBKDF2 key hashes, active managed client certificate fingerprints, per-certificate lifecycle records with source/serial/subject/issuer/validity/removal metadata, and computed `certificate_health` expiry and renewal summaries, returning generated keys only when creating key-backed identities, supporting certificate-only identities without an implicit fallback key, supporting revoke, CSR certificate issuance and renewal through either configured PEM CA files or an external CA/KMS/HSM issuer command, audited add/remove rotation for managed certificate fingerprints, audited certificate health scans through `POST /api/v1/device-identities/certificates/scan` with `device_identity.certificates_scanned` semantic events, operator-readable rendered summaries, filtered Bot Gateway fan-out, configurable scan-worker notification routing for expiring/expired/unknown certificate inventory, renewal-due/overdue scan counters, optional background scan execution through `AGENTBRIDGE_DEVICE_CERT_SCAN_WORKER_ENABLED`, rejecting known expired managed certificates during fingerprint authentication, optional `allowed_resource_ids` for REST path/query/top-level JSON body and WebSocket route resource allowlists, authorizing REST/WebSocket clients through device keys or proxy-verified certificate fingerprints, enforcing coarse transport scopes plus `audit_read` for audit/event history HTTP reads, `bot_gateway_read` for Bot Gateway HTTP reads, `bot_gateway_manage` for Bot Gateway HTTP mutations, `onebot_event_ingest` for OneBot inbound events, `command_parse` for command parsing, `command_execute` for command execution, `device_manage` for device-identity administration, `policy_read` for access/approval policy reads and simulation, `policy_manage` for access/approval policy administration, `group_role_read` for chat-context role reads, `group_role_manage` for chat-context role administration, `chat_context_manage` for chat-context creation/pointer writes, `project_read` for project/workspace reads, `project_manage` for project/workspace/binding writes, `session_read` for session, queued-Turn, and current writer-lease reads, `session_manage` for session lifecycle, writer-lease writes, queue claim-next, queue clears, queue reorders, and queue pause/resume, `session_send` for direct session turn enqueue and queued-turn removal, `session_event_ingest` for Terminal Agent session event ingestion, `interaction_read` for interaction/approval reads, `interaction_manage` for interaction/approval writes, `terminal_read` for terminal-read HTTP operations, and `terminal_control` for terminal-control HTTP operations, and tracking `last_used_at` on successful managed-device authentication.
- Managed device resource allowlists resolve Bot Gateway delivery result/edit/delete mutation `idempotency_key` values to the stored delivery record's `chat_context_id` before authorization, allowing adapter credentials with `bot_gateway_manage` to be constrained to one chat context even when the mutation payload only names the delivered message.
- Optional trusted-proxy client certificate fingerprint gate through `AGENTBRIDGE_CLIENT_CERT_FINGERPRINTS` or `AGENTBRIDGE_CLIENT_CERT_FINGERPRINTS_FILE`, authorizing REST APIs, WebSocket routes, and Admin Web pages from mTLS-verified clients while rereading fingerprint files per request/connection and failing closed on unreadable or empty files.
- SQLAlchemy-backed repository enabled with `AGENTBRIDGE_DATABASE_URL`, with environment-tunable runtime engine pool options for size, overflow, timeout, recycle, pre-ping, SQL echo diagnostics, and an optional same-host single-writer snapshot lock through `AGENTBRIDGE_DATABASE_WRITE_LOCK_PATH`; locked writers reload the latest database state before applying a mutation and writing the updated snapshot.
- Database deployment guide under `docs/operations/DATABASE_DEPLOYMENT.md`, covering SQLite/PostgreSQL URLs, explicit Alembic migration flow, runtime pool settings, and the current snapshot persistence boundary.
- Alembic initial migration for projects, workspaces, chat contexts, bindings, sessions, turns, interactions, writer leases, command idempotency records, audit events, and semantic events.
- Recovery tests proving persisted control-plane state survives repository re-instantiation.
- Terminal Agent input gateway with fake, tmux, and stdlib PTY backends.
- Terminal starts that omit an explicit command now resolve from the Session `agent_type`, defaulting to `claude`, `codex`, or `sh` for `generic_tui`, with `AGENTBRIDGE_AGENT_CLAUDE_COMMAND`, `AGENTBRIDGE_AGENT_CODEX_COMMAND`, and `AGENTBRIDGE_AGENT_GENERIC_TUI_COMMAND` overrides; `terminal.started` records the resolved command, agent type, and command source for lifecycle recovery and operational review, while lifecycle status, local daemon status, and the Terminal Lifecycle Admin page expose PATH/executable-file readiness for each launch profile without executing the configured CLI; terminal-control operators can explicitly run bounded version probes through `POST /api/v1/terminal/agent-launch/probe`, local daemon `probe_agent_launch_profiles`, or the Admin page, using built-in Claude/Codex `<executable> --version` probes or `AGENTBRIDGE_AGENT_*_VERSION_COMMAND` overrides. Structured adapter capability detection is now exposed through `POST /api/v1/terminal/agent-adapters/detect`, local daemon `detect_agent_adapters`, and the Admin page; Claude/Codex stay gated as not ready unless an explicit `AGENTBRIDGE_AGENT_*_HANDSHAKE_COMMAND` returns JSON with protocol `agentbridge.adapter.v1`, a compatible flag, schema version, and capability list. Adapter schema matrices now include provider-version verification evidence, and capability detection reports whether the observed provider version is verified, unknown, or unverified without silently treating unknown provider versions as proven.
- PTY backend launches local commands with `pty.openpty` and `subprocess.Popen`, owns process-group cleanup, reads the PTY master fd on a background thread, and exposes cursor-based output chunks.
- PTY output retention is bounded by `AGENTBRIDGE_TERMINAL_PTY_OUTPUT_LIMIT_CHARS` (default `1000000`); stale cursors receive reset chunks with the retained tail instead of allowing unbounded memory growth.
- PTY backend can persist a local JSON host-state registry through `AGENTBRIDGE_TERMINAL_PTY_HOST_STATE_PATH`, recording session ID, cwd, command, host pid, child pid, process status, exit code, and output cursor metadata for future host supervision.
- Independent `agentbridge-pty-host` process that owns the stdlib PTY backend behind a chmod `0600` Unix socket, with `AGENTBRIDGE_TERMINAL_PTY_HOST_TOKEN_FILE` hot reload for host/client token rotation, plus `AGENTBRIDGE_TERMINAL_BACKEND=pty_host` client backend for API/daemon processes to reconnect to host-owned PTYs after restart.
- `pty_host` client backend can auto-start `agentbridge-pty-host` with `AGENTBRIDGE_TERMINAL_PTY_HOST_AUTO_START=true`; it removes Unix socket files only when health probing proves there is no listener, waits for host health, and retries the original request once.
- `pty_host` supervisor treats `PERMISSION_DENIED`, timeout, protocol, permission, and other ambiguous health probe failures as evidence that the socket should be preserved; it records `last_error` and fails closed instead of starting a competing host process.
- `pty_host` supervisor can run an optional API/daemon-lifecycle watchdog with `AGENTBRIDGE_TERMINAL_PTY_HOST_WATCHDOG_ENABLED=true`, periodically checking host health and restarting `agentbridge-pty-host` after a crash.
- When PTY host watchdog and `AGENTBRIDGE_TERMINAL_AUTO_RESTART_ON_LOST=true` are both enabled, the lifecycle monitor can detect PTY sessions lost by host-process death, emit `terminal.lost`, enter offline protection, and restart them from the latest persisted `terminal.started` command only when it matches the configured auto-restart command allowlist; successful restart emits `terminal.started` and exits offline protection.
- Terminal lifecycle status now includes backend supervision state and automatic restart policy state, exposing whether a PTY host watchdog is enabled/running, how many host restarts it has performed, configured command allowlist patterns, restart attempts, and policy blocks.
- PTY host service-manager deployment guide and templates for systemd user services and macOS launchd user agents live under `docs/operations/`, including mode-0600 token-file handling and recovery expectations.
- Codex app-server proxy deployment guide and wrapper templates live under `docs/operations/CODEX_APP_SERVER_PROXY_SERVICE_MANAGER.md` and `docs/operations/templates/`, documenting the correct stdio topology, mode-0600 adapter credential handling, health/response JSONL state files, PTY-host service-manager layering, and why the proxy must remain in the upstream client's stdin/stdout path instead of running as a detached background daemon.
- Local Terminal Agent daemon using JSONL over a Unix socket with token authentication, token-file hot reload for local rotation, chmod `0600` socket permissions, and default same-OS-user peer UID verification where Unix peer credentials are available.
- Local daemon actions for `health`, `lifecycle_status`, `run_lifecycle_monitor_once`, `replay_events`, `ack_events`, `probe_agent_launch_profiles`, `detect_agent_adapters`, `start_session`, `restart_session`, `acquire_human_lease`, `release_lease`, `set_offline_protection`, `claim_next_turn`, `submit_input`, `snapshot`, `status`, cursor-based `read_output`, and multi-frame `stream_output`.
- Local daemon `replay_events` returns Session semantic events after an `after_seq` cursor with a bounded limit and `last_seq`, and `ack_events` can save a local consumer cursor so subsequent daemon reconnects resume after the Control Plane's stored ACK.
- Local Terminal Agent client waits briefly for Unix socket recovery, allowing console requests to survive short daemon restart windows.
- Lifecycle tests cover local daemon socket restart/reconnect behavior and tmux backend reuse of existing sessions after Agent restart, with an opt-in real-tmux smoke test gated by `AGENTBRIDGE_RUN_TMUX_TESTS=true`.
- Local Console Client command `agentbridge-console`.
- Console Client acquires a human writer lease on first input, caches the epoch, forwards text/paste/signal/resize through the daemon, and can release the lease on exit.
- Console Client raw TTY passthrough mode with safe terminal-state restoration, initial resize forwarding, `SIGWINCH` resize forwarding, Ctrl-C/Ctrl-D signal mapping, and Ctrl-] detach.
- Terminal backends expose cursor-based output chunks through `read_output(after_cursor)`, with fake/tmux implementations backed by current snapshot state and the PTY backend backed by its reader loop buffer.
- Console Client raw mode follows terminal output through daemon `stream_output` frames, appending cursor chunks and repainting when the backend reports a reset.
- Terminal status is exposed through REST, Terminal WebSocket, and local daemon actions; PTY status includes running/exited state, exit code, pid, and current output cursor.
- Terminal Agent emits `terminal.exited` once per observed terminal start generation when status or lifecycle polling first sees a backend has exited, and the renderer has an operator-visible fallback for that lifecycle event.
- Terminal lifecycle monitor can poll known started terminals in the background; the local Terminal Agent daemon enables it by default, and the FastAPI lifespan can enable it with `AGENTBRIDGE_TERMINAL_LIFECYCLE_MONITOR_ENABLED=true`.
- Terminal lifecycle monitor status and manual run-once operations are exposed through `GET /api/v1/terminal/lifecycle-monitor`, `POST /api/v1/terminal/lifecycle-monitor/run-once`, and local daemon lifecycle actions for operational inspection.
- Managed device identities require `terminal_read` for terminal snapshot/status and lifecycle status HTTP reads.
- Terminal lifecycle tracking is reconstructed from persisted semantic events at service startup, restoring known terminal start generations and already-reported exits after Control Plane or daemon process restarts.
- Recovered terminal generations whose backend state is missing emit `terminal.lost` once per generation, enter offline protection once per lost generation, and keep the projection recoverable from persisted events with an operator-visible renderer fallback.
- Terminal restart can be requested through REST, Terminal WebSocket, and the local daemon; if no command is supplied, it reuses the latest persisted `terminal.started` command, records a new start generation, and avoids replacing an already-running backend.
- Terminal lifecycle policy can automatically restart lost recovered terminals with `AGENTBRIDGE_TERMINAL_AUTO_RESTART_ON_LOST=true`, capped by `AGENTBRIDGE_TERMINAL_AUTO_RESTART_MAX_ATTEMPTS` to prevent restart loops and constrained by `AGENTBRIDGE_TERMINAL_AUTO_RESTART_COMMAND_ALLOWLIST`; blocked command replays emit `terminal.auto_restart.skipped` and leave the Session in offline protection.
- Local Terminal Agent can open a visible desktop console after `start_session` through `AGENTBRIDGE_TERMINAL_AUTO_OPEN`, either with a custom `AGENTBRIDGE_TERMINAL_OPEN_COMMAND` template or built-in `AGENTBRIDGE_TERMINAL_OPEN_PRESET` values.
- Built-in desktop terminal presets support `auto`, `macos-terminal`, `gnome-terminal`, `konsole`, `wezterm`, `alacritty`, `kitty`, and `xterm`; token/socket state is kept out of launched argv and supplied through environment variables or a short-lived local launcher script for macOS Terminal.
- Terminal input request idempotency now prevents duplicate backend writes for repeated request IDs.
- Terminal start/input/snapshot/status REST endpoints for MVP integration tests.
- Terminal command WebSocket through `/api/v1/sessions/{id}/terminal/ws`, supporting `health`, `replay_events`, `ack_events`, `flush_event_outbox`, `start_session`, `restart_session`, `acquire_lease`, `release_lease`, `claim_next_turn`, `submit_input`, `snapshot`, and `status`; `replay_events` uses the same `after_seq`/`last_seq` recovery contract as REST event replay and local daemon replay while remaining a read-side action guarded by Session view permission, can resume from a saved `ack_events` consumer cursor when `after_seq` is omitted, and can explicitly flush the Terminal Agent lifecycle event outbox through the same terminal-control permission boundary as REST/Admin.
- Terminal input enforcement against current writer lease owner and epoch, with rejected/accepted semantic events.
- RenderDocument intermediate representation for bot-facing messages.
- OneBot/plain-text fallback renderer with code block preservation, action listing, tool progress summaries, and deterministic message splitting; overlong fenced code blocks, including Markdown-authored fences, are split into length-safe messages with balanced code fences in every chunk.
- Operator-visible renderer fallback for skipped terminal auto-restarts when a recovered command is not allowlisted.
- Rendered event APIs through `GET /api/v1/sessions/{id}/rendered-events` and `GET /api/v1/events/rendered`.
- Bot Gateway delivery service using the renderer.
- In-memory Bot transport and idempotent delivery records keyed by platform, chat context, event, and message index.
- Delivery APIs through `POST /api/v1/bot-gateway/deliver-session-events`, `POST /api/v1/bot-gateway/deliver-events`, and `GET /api/v1/bot-gateway/deliveries`.
- Bot Gateway subscriber WebSockets through `/api/v1/bot-gateway/session-events/ws` and `/api/v1/bot-gateway/notifications/ws`, pushing ordinary `bot.render.create` frames, preserving Bot Gateway protocol frame types such as `bot.interaction.ack`, and streaming cross-flow `bot.notification` frames by chat context, with chat routing metadata, per-message idempotency keys, per-message update keys for adapter-side edit/merge behavior, and `after_event_id` replay for chat-context notification reconnects that span system, project, and session streams.
- Bot render frames include platform-neutral action descriptors for button-, select-, and modal-capable adapters, with callback payloads that route back into audited `/agent` commands; question requests with options now expose select descriptors, and plan requests expose callback-safe approve/show/cancel descriptors plus a modal revision descriptor with command-template input metadata. Successful platform interaction submissions emit idempotent `bot.interaction.ack` semantic events on the affected session stream.
- Bot delivery result API through `POST /api/v1/bot-gateway/delivery-results`, tracking platform `acknowledge`, `edit`, and `delete` results by delivery idempotency key.
- Bot Gateway outbound mutation APIs through `POST /api/v1/bot-gateway/deliveries/edit` and `/delete`, calling the selected transport before updating platform delivery state.
- Bot delivery edit/delete result updates emit `bot.render.update` or `bot.render.delete` semantic events carrying delivery metadata and original event linkage for Bot Gateway WebSocket subscribers.
- Bot delivery records are now domain/repository state and persist through Alembic migration `0002_bot_delivery_records`.
- Alembic migration `0006_bot_delivery_platform_state` persists platform delivery lifecycle columns.
- Recovery tests prove replay after repository re-instantiation skips already delivered Bot messages.
- OneBot V11 HTTP transport with group/private payload routing, `delete_msg`, bearer token support, idempotency header, and an optional platform-specific message edit extension through `AGENTBRIDGE_ONEBOT_EDIT_ACTION` plus `AGENTBRIDGE_ONEBOT_EDIT_MESSAGE_FIELD`.
- Bot transport selection through `AGENTBRIDGE_BOT_TRANSPORT=onebot.v11` and `AGENTBRIDGE_ONEBOT_HTTP_URL`.
- OneBot V11 inbound event adapter for group/private text messages, reply segments, command-carrying action callbacks, select submissions, and modal-style `command_template` submissions, including nested render action descriptor payloads such as `data.payload.command` or `payload.command`; inbound messages, commands, button clicks, select submissions, modal submissions, and attachment-bearing messages emit idempotent Bot Gateway upstream semantic events (`bot.message.received`, `bot.command.received`, `bot.action.clicked`, `bot.selection.submitted`, `bot.modal.submitted`, and `bot.attachment.received`) before command execution when applicable. Callback events must include the clicking/submitting `user_id`, so approval/answer commands re-enter RBAC/access-policy checks as the concrete OneBot actor instead of trusting the platform payload. Text-only fallback can now resolve `/agent answer ...`, `/agent approve ...`, `/agent deny ...`, `/agent plan show/approve/revise/cancel ...`, and localized `/agent 使用 <session>` replies/commands through the same audited command path; replies that omit the Interaction ID are mapped through Bot delivery records back to the delivered semantic event's `interaction_id`; action/select/modal submissions return an `ack_event` and append the same `bot.interaction.ack` event idempotently by platform event id.
- Platform-neutral Bot Gateway upstream API through `POST /api/v1/bot-gateway/inbound-events`, accepting design-document `bot.message.received`, `bot.command.received`, `bot.slash_command.received`, `bot.action.clicked`, `bot.selection.submitted`, `bot.modal.submitted`, and `bot.attachment.received` events, recording them idempotently, and executing command-like events through the same command parser when command text is present.
- Bot Gateway command registration manifest through `GET /api/v1/bot-gateway/command-registration-manifest`, generated from the same structured command registry so native slash-command/menu adapters do not hand-maintain command metadata separately from `/agent` parsing.
- Bot Gateway command registration result API through `POST /api/v1/bot-gateway/command-registration-results`, recording platform native slash-command/menu registration outcomes as idempotent `bot.command_registration.result` semantic events with platform, scope, command list, status, error, and raw adapter payload metadata.
- OneBot inbound API through `POST /api/v1/onebot/events`, converting `/agent` and `/ab` messages or action callbacks into the existing command execution flow and requiring `onebot_event_ingest` for managed device credentials.
- Optional NoneBot wrapper module that normalizes NoneBot/OneBot-style message events into the existing command execution flow without adding a hard NoneBot dependency.
- Dependency-free NoneBot matcher/lifecycle registration helpers that attach the AgentBridge async handler through one or more matcher `handle()` decorators and can bind startup command registration to the same plugin instance.
- NoneBot command-registration helpers that expose the shared Bot Gateway command manifest, wire dependency-free `driver.on_startup()` registration hooks around sync or async native registrars, and record native menu/command registration outcomes or startup registration failures as idempotent `bot.command_registration.result` semantic events without requiring a hard NoneBot runtime dependency.
- Dependency-free NoneBot outbound transport for Bot Gateway text delivery and deletion on `onebot.v11`, supporting application wrappers with `send_to()`/`send()` and NoneBot-style async or sync `call_api()` fallbacks.
- NoneBot callback/action payloads containing render action descriptor commands can execute through the same audited `/agent` command path using the shared OneBot callback payload parser.
- Bot delivery failure records with attempt count, last error, next retry time, and exponential backoff.
- Retry API through `POST /api/v1/bot-gateway/retry-failed-deliveries`.
- Alembic migration `0003_bot_delivery_retry_metadata` adds retry metadata columns.
- Tests cover initial failure, due retry, and retry after repository restart.
- Background Bot delivery retry worker with environment-controlled startup.
- Retry worker status and bounded run-once APIs through `/api/v1/bot-gateway/retry-worker`.
- Retry worker scheduling honors each record's `next_retry_at` and caps retry throughput by configured batch size and interval.
- Platform-scoped Bot delivery rate-limit policies configured through `AGENTBRIDGE_BOT_RATE_LIMITS`.
- Rate-limited Bot messages are stored as `retrying` records with `next_retry_at` without consuming send attempts.
- Rate-limit policy API through `GET /api/v1/bot-gateway/rate-limits`.
- Bot Gateway platform capability API through `GET /api/v1/bot-gateway/capabilities`, exposing the design-document `BotCapability` fields plus `platform` and `deleteMessage` for standard OneBot V11 and plain-text contracts.
- Bot Gateway now treats platform limit responses with `retry_after_seconds` as adaptive `retrying` deliveries and schedules `next_retry_at` from the observed delay.
- OneBot HTTP outbound parses HTTP 429/`Retry-After` and reset headers into adaptive Bot Gateway retry metadata.
- Control Plane interaction APIs for questions, approvals, and plan checkpoints.
- REST interaction routes: create, list, show, answer, and vote.
- `/agent approvals`, `/agent approval show`, `/agent question show/list`, `/agent answer`, `/agent approve`, `/agent deny`, and `/agent plan show/list/approve/revise/cancel`.
- Basic approval quorum handling with `pending`, `partially_approved`, and `resolved` states.
- Approval request and vote semantic events with Bot-rendered plain-text actions; plan request events render plan-specific approve/show/cancel actions plus a revise feedback command.
- Interaction expiration through `expires_at` or API `ttl_seconds`, with `interaction.expired` events.
- Interaction cancellation through `POST /api/v1/interactions/{id}/cancel` and `/agent approval cancel`.
- Expired and cancelled interactions are persisted and cannot be answered or approved afterward.
- Risk-aware approval policy for low/medium/high/critical interactions.
- `AGENTBRIDGE_APPROVAL_QUORUMS` can override default risk quorum.
- High and critical approvals require `approval.dangerous`, exposed through the `dangerous_approver` role.
- Approval interactions store requester, risk level, required quorum, and policy snapshot.
- Project and chat-context scoped approval quorum overrides with `chat_context > project > global` precedence.
- Approval policy management REST APIs through `GET/PUT /api/v1/projects/{id}/approval-policy` and `GET/PUT /api/v1/chat-contexts/{id}/approval-policy`.
- `/agent policy show` and `/agent policy set <risk> <quorum>` for group/project approval quorum management.
- Alembic migration `0005_approval_policy_overrides` persists approval policy overrides.
- Explicit chat-context role bindings for group users.
- Effective actor roles now merge request/default roles with persisted group role bindings before permission checks.
- `/agent role list/grant/revoke` commands for maintainers/admins.
- Role binding REST APIs through `GET /api/v1/chat-contexts/{id}/roles`, `POST /api/v1/chat-contexts/{id}/roles/grant`, and `POST /api/v1/chat-contexts/{id}/roles/revoke`.
- Alembic migration `0004_group_role_bindings` persists role bindings.
- OneBot inbound permissions can now start from default `member` roles and rely on group bindings for `operator` capabilities.
- Persistent access policy rules with `allow`/`deny` effects over action, resource type/id, actor IDs, roles, and exact-match attributes.
- PolicyEngine evaluates enabled access policy rules before RBAC fallback; explicit deny rules win over allow rules.
- Access policy management REST APIs through `GET/POST/PUT /api/v1/access-policy/rules` and `POST /api/v1/access-policy/rules/{id}/delete`.
- Policy simulation REST API through `POST /api/v1/access-policy/simulate`, returning decision source, reason, required permission, roles, and matched rule ID.
- Built-in admin entrypoint at `/admin`, system health dashboard at `/admin/system`, project/session operations dashboard at `/admin/projects`, interaction/approval operations dashboard at `/admin/interactions`, audit/event exploration dashboard at `/admin/audit`, access policy editor at `/admin/access-policy`, terminal lifecycle dashboard at `/admin/terminal-lifecycle`, device identity management dashboard at `/admin/device-identities`, and Bot delivery operations dashboard at `/admin/bot-delivery`; focused route coverage verifies the admin pages link to the underlying operational APIs.
- System Health Admin Web page gives a read-only operational summary of `/api/v1/health`, `/api/v1/readiness` MVP acceptance pass/warn/fail counts with warn/fail next-step action items, acceptance evidence gap summaries, terminal lifecycle monitor status, Bot retry worker status, certificate scan worker status, Bot platform capabilities, Bot rate-limit policies, and managed-device endpoint reachability, and can export the current endpoint/readiness snapshot plus readiness action evidence summaries as JSON acceptance evidence.
- Project/Session Admin Web page can export the current project inventory, selected project/workspace state, chat-space project bindings, session details, queue state, writer leases, and pending-approval summaries as JSON acceptance evidence.
- Interaction Admin Web page can export filtered interaction lists plus the selected question/approval/plan detail, actor context, chat context, votes, answers, cancellations, and expiry state as JSON acceptance evidence.
- Terminal Lifecycle Admin Web page shows tracked sessions, exit/loss counts, auto-restart attempts, command allowlist patterns, policy blocks, backend supervision state, Terminal Agent lifecycle event outbox pending/read/last-flush state, Agent launch readiness/version probe/adapter capability results, provider-version verification summaries for structured adapters, bounded lifecycle run-once/outbox-flush/probe/detect actions, and JSON export of the current lifecycle/probe/adapter evidence snapshot.
- Audit/event Admin Web page exposes the cross-stream semantic event search API with project, session, type, source, trace, turn, interaction, query text, created-at time-window, and limit filters.
- Audit/event Admin Web page can live-tail a selected session's semantic event stream over `/api/v1/sessions/{id}/events/ws`, while retaining manual REST replay for bounded event history inspection.
- Device Identity Admin Web page lists active/revoked managed devices with allowed scopes, allowed resource IDs, certificate fingerprint count, certificate health/expiry/renewal summary, certificate lifecycle records in selected JSON details, and last-used timestamps, creates or rotates device keys, edits resource allowlists and managed certificate fingerprints, issues or renews CA-backed certificates from CSR PEM, runs add/remove certificate fingerprint rotation, runs audited certificate health scans, displays generated keys/certificates/scan results once, revokes selected device identities through the managed device API, and exports a redacted JSON evidence snapshot covering device scopes, resource allowlists, certificate health, and latest management operation metadata.
- Bot Delivery Admin Web page lists delivery records, shows retry worker state, displays platform capability contracts including configured OneBot edit/delete support, shows recent native command registration result telemetry, shows rate-limit policies, retries due failures, runs a bounded retry worker pass, can edit or delete a selected delivery through the Bot Gateway mutation APIs, and exports delivery records, worker state, platform capabilities, rate limits, command-registration telemetry, and latest mutation/retry action as JSON acceptance evidence.
- Managed device identities now require `audit_read` for audit records, audit exports, semantic event search/replay, and rendered event replay over HTTP.
- Managed device identities now require `bot_gateway_read` for Bot Gateway delivery, capability, rate-limit, and retry-worker HTTP reads.
- Managed device identities now require `onebot_event_ingest` for OneBot inbound event delivery through `POST /api/v1/onebot/events`.
- Managed device identities now require `command_parse` for command parsing through `POST /api/v1/commands/parse`.
- Managed device identities now require `policy_read` for access/approval policy reads and access-policy simulation.
- Managed device identities now require `group_role_read` for chat-context role binding reads.
- Managed device identities now require `chat_context_manage` for chat-context REST writes: create contexts and update active project/session pointers.
- Managed device identities now require `project_read` for project/workspace/binding REST reads: list/show projects, list workspaces, and read chat-space project bindings.
- Managed device identities now require `session_read` for session REST reads: list/show sessions, queued Turns, and current writer leases.
- Managed device identities now require `session_manage` for session REST writes: create/close sessions, acquire/release leases, clear queues, reorder queued Turns, and pause/resume queues.
- Managed device identities now require `session_send` for direct session turn enqueue and queued-turn removal through `POST /api/v1/sessions/{id}/turns` and `DELETE /api/v1/sessions/{id}/queue/{turn_id}`.
- Managed device identities now require `session_event_ingest` for Terminal Agent event ingestion through `POST /api/v1/sessions/{id}/events`, structured Adapter event ingestion through `POST /api/v1/sessions/{id}/agent-adapter/events`, and Adapter interaction-response polling through `GET /api/v1/sessions/{id}/agent-adapter/responses`.
- Managed device identities now require `interaction_read` for interaction/approval REST reads: list and show.
- Managed device identities now require `interaction_manage` for interaction/approval REST writes: create, answer, cancel, and vote.
- Optional `AGENTBRIDGE_ADMIN_TOKEN` or `AGENTBRIDGE_ADMIN_TOKEN_FILE` browser gate for `/admin*` pages, accepting one-time `admin_token` query unlock, HttpOnly/SameSite cookie sessions, bearer tokens, or `X-AgentBridge-Admin-Token` headers.
- Alembic migration `0007_access_policy_rules` persists access policy rules.
- Alembic migration `0008_semantic_event_query_columns` adds indexed semantic event routing columns for source, trace, project, session, turn, and interaction search.
- Alembic migration `0009_device_identities` persists managed device identities.
- Alembic migration `0010_event_created_at_columns` adds indexed audit and semantic event `created_at` columns and backfills them from legacy JSON payloads.
- Alembic migration `0011_audit_event_query_columns` adds indexed audit event trace/project/session/interaction/chat-context query columns and backfills them from legacy JSON payloads.
- Alembic migration `0012_event_payload_search_columns` adds indexed normalized audit details and semantic event payload search-text columns and backfills them from legacy JSON payloads.
- Project, session, interaction, approval, group-role, policy-management, and terminal checks now pass resource type/id plus stable attributes into access policy evaluation.
- Terminal REST and WebSocket paths reuse Control Plane `terminal` resource checks, so session-specific terminal rules are enforced outside the main command flow as well.
- Focused unit/API tests for the above.

Not implemented yet:

- Remaining PTY host hardening: true process-preserving recovery across host-process death, cross-platform stale socket/pipe cleanup, and Windows ConPTY/Named Pipe parity.
- Richer OneBot renderer/action adapter and deeper native NoneBot lifecycle helpers beyond matcher/lifecycle bootstrap, command registration, and the dependency-free outbound transport.
- Native Claude Code Hook and Codex app-server adapters beyond the current Session agent launch command profiles, non-executing launch readiness checks, bounded version probes, explicit external handshake/schema gate contract, external adapter event normalization endpoint, adapter-driven Interaction state creation, response polling endpoint, reusable `agentbridge-adapter-client` bridge clients with blocking response waits, Claude Hook command shim/stdout formatting, Claude Hook settings generation/merge, Codex JSON-RPC message/JSONL stream/stdout-proxy shims with action envelopes/response wrappers, live Codex app-server response injection, bounded Codex app-server proxy restart supervision, health JSONL for long-running proxy monitoring, proxy service-manager deployment guidance/wrapper templates, provider-captured Codex schema projection, schema compatibility/snapshot matrix, and provider-version verification reporting, including broader verified per-version behavior matrices beyond the first Codex v1 provider capture.
- Broader Admin Web UI beyond system health, project/session operations, interaction/approval operations, audit/event live exploration, access policy, terminal lifecycle, device identity management, and Bot delivery operations.
- Provider-native audit archive signing clients and richer verifier distribution/attestation workflows beyond the current external signer command, local HMAC-SHA256, local PEM asymmetric signing paths, and offline `agentbridge-audit-verify` verifier.
- Production API/WebSocket hardening with provider-native CA SDK clients, managed CA/offline verifier tooling, native TLS termination guidance, and deeper command/payload-aware or method-specific resource/action device authorization beyond the current token-file/device-key/client-cert-fingerprint, transport-scope, PEM-CA or external-issuer CSR issuance/renewal, managed fingerprint rotation, certificate metadata inventory, audited certificate health scans, renewal scheduling signals, background certificate scan worker, operator-readable scan event rendering, filtered/configured Bot Gateway scan fan-out, certificate expiry/renewal health summaries, the device certificate operations runbook, and visible resource-ID gates.
- Platform-specific rich card/button transport adapters and richer outbound mutation extensions beyond standard OneBot V11 plus the current configurable OneBot edit action.
- Native platform-specific rich action/callback support for platforms that expose buttons, selects, modals, or interaction tokens beyond the current command-carrying descriptor callbacks.
- Broader per-adapter action callback state beyond command-carrying callbacks.
- Fully normalized relational query layer for arbitrary payload search and complex audit/event cross-field searches; audit and semantic event routing, created-at filters, normalized payload/details text search, and safe candidate narrowing for simple dot-path exact field filters now run in the database where supported, while final field semantics are verified after model hydration, `q` remains a bounded contains match, and complex JSON predicates are still outside the current query model.
- Further SQLAlchemy hardening with row-level writes, stronger transaction boundaries, cross-host writer coordination, and higher-throughput multi-writer behavior beyond the current same-host single-writer snapshot lock.

## Important Decisions

- The first backend slice uses an in-memory repository to make command, routing, lease, and API semantics testable before introducing persistence.
- Unknown ASCII-looking `/agent` management commands are rejected instead of being silently treated as prompts. Non-command free text still becomes `ask` to support the documented shortcut pattern.
- Numbered `/agent select project|session` is intentionally a separate command instead of overloading `project use 1` or `session use 1`, so numeric project names, aliases, IDs, or short codes keep their normal resolver semantics while text-only users still get an explicit numbered recovery path after list commands.
- Plain `session use` and localized `使用` are Session-token shortcuts, not project-scoped selectors. A successful selection updates both active Session and active Project to the Session's owning project so later Turn routing cannot keep a stale project pointer.
- Numeric Interaction arguments are treated as transient list selectors, not persisted short codes. They are resolved only from the current pending, type-specific Interaction list visible to the actor, while non-numeric Interaction IDs keep the existing exact lookup behavior for historical/resolved records.
- The text `message` returned by project, session, and type-specific Interaction list commands is the authoritative user-visible source for numbered selectors; the structured `data` payload keeps UUIDs and short codes for richer clients.
- A chat context can bind multiple projects but only one binding may be default. Promoting a new default updates the active project pointer for new routing decisions, but it does not migrate existing Sessions or their Workspaces.
- Command idempotency caches successful command results only. Execution-stage failures still emit `command.failed` audit records on each attempt so retry behavior remains explicit and transient target/permission/quota/lease errors are not frozen by an idempotency key.
- Semantic events are separate from audit records: events drive product state replay and Bot rendering, while audit records preserve security/accountability history.
- Audit listing is a repository concern. In-memory and SQLAlchemy repositories return bounded newest-first results; the SQLAlchemy path filters action, actor, trace, project, session, interaction, created-at windows, normalized details text, and safe dot-path details-field candidates in the database, then applies the shared exact field matcher until the requested limit is reached.
- Audit archive signing now prefers an external command signer before local PEM or HMAC signing, so production deployments can bridge to KMS/HSM/Vault without loading non-exportable keys into AgentBridge. The external signer receives the canonical archive on stdin plus digest/key metadata in environment variables and returns JSON signature metadata for offline verification. `agentbridge-audit-verify` independently canonicalizes signed archives, verifies `archive_sha256`, and checks HMAC, Ed25519, RSA-PSS-SHA256, ECDSA-SHA256, or KMS-style RSA-PSS labels with a supplied HMAC key, public key, or certificate. Provider-native SDK clients and richer verifier distribution/attestation workflows remain future hardening.
- Semantic event replay and semantic event search are separate repository contracts. Replay APIs preserve per-stream ascending `seq` behavior for clients, while `/api/v1/events` and `list_semantic_events` provide bounded newest-first operational search across streams using SQLAlchemy routing, created-at, normalized payload text, database-assisted dot-path payload field candidates, and shared exact field verification. The current `q` search and field filters are useful for operations but do not replace a full JSON predicate query layer.
- SQLAlchemy persistence is currently a write-through snapshot repository. Runtime connection pooling, migration deployment, and an optional same-host single-writer lock are documented; when `AGENTBRIDGE_DATABASE_WRITE_LOCK_PATH` is configured on every local API/daemon writer, mutating operations serialize through that POSIX file lock, reload the latest database state, and then write the updated snapshot. This prevents stale locked writers from overwriting each other, but row-level updates and stronger database transaction boundaries are still needed for high-throughput or cross-host multi-writer deployments.
- Terminal input must pass through the AgentBridge gateway. Direct `tmux attach` remains outside the safety model because it bypasses writer leases and Workspace write-capacity checks.
- Human writer control does not silently discard ordinary Bot/chat tasks. While a human lease owns the Session, `turn.enqueue` accepts permitted work into the normal queue with `queue_reason=human_control` and visible lease context; releasing that human lease emits a queue-unblocked semantic event so Bot adapters can resume the same Session from the existing queue. Low-level terminal input remains epoch-gated and stale Bot terminal writes are rejected.
- Project `max_active_sessions` counts non-closed, non-archived, non-error sessions and is enforced before Workspace selection so over-quota requests fail without allocating short codes, sessions, or leases. Project `max_queued_turns` counts queued Turns across all sessions in the project and is enforced before Turn allocation; the returned `queue_position` shows where the rejected Turn would have landed. Project `max_running_turns` is enforced when Terminal Agent ingests `turn.started`; `turn.completed`, `turn.failed`, and `turn.interrupted` release the running slot and update the owning Session active Turn pointer. Project `daily_turns_per_user` counts all Turns queued by the same actor in the same project on the current UTC date; completed/failed/cancelled Turns still consume that daily creation quota until `reset_at`.
- Queue removal is intentionally limited to queued Turns. It marks them `cancelled` so they stop consuming queued capacity while preserving audit and event history; running Turns remain controlled by stop/interrupt lifecycle events rather than queue removal.
- Queue ordering uses a dedicated `queue_order` field rather than mutating `queued_at`, so reorder operations do not corrupt daily Turn quota accounting. Queue list responses return an opaque `queue_version` and `queue_paused`; reorder and pause/resume require the version, remove accepts it for compare-and-swap protection, clear requires both the version and a `confirm_count`/`--confirm <count>` matching the current queued Turn count, and paused queues reject new `turn.started` lifecycle events until resumed.
- Claiming the next queued Turn is an executor/control-plane operation, not a chat fallback command. It requires `session.manage`, optionally checks the same `queue_version`, starts only the current queue head, and reuses the existing `start_turn` invariants so paused queues, active running Turns, terminal states, and project running quotas cannot be bypassed by an adapter racing the Bot. When an executor also requests prompt submission, Terminal Agent checks the terminal is already running and then uses the normal writer-lease and terminal-input path, so human lease preemption and stale epoch rejection remain the single enforcement point.
- `WorkspaceType.READ_ONLY` is a backend-enforced write-policy signal: creation requests are normalized to `is_writable=false` and `max_write_sessions=0` before lease enforcement.
- The local Terminal Agent socket is token-gated, chmodded to `0600`, checks same-user Unix peer credentials by default, and can reread `AGENTBRIDGE_LOCAL_TOKEN_FILE` per request for local token rotation. Windows named-pipe parity and broader production credential lifecycle policy remain future hardening work.
- Local console/daemon clients open a fresh socket per request and retry connection for short restart windows. Long offline periods still need explicit user-facing reconnect state during raw TTY passthrough.
- Console raw mode is still an input/control passthrough over the Terminal Agent socket, not a full terminal emulator. Its daemon output stream consumes backend cursor chunks; with `AGENTBRIDGE_TERMINAL_BACKEND=pty` those chunks come from a stdlib PTY reader loop, while fake/tmux remain snapshot-derived.
- The stdlib PTY backend is opt-in for local experiments. Fake remains the default test backend, and tmux remains the resumable MVP backend after Agent process restarts.
- A terminal start request without an explicit command is intentionally agent-aware: Claude Sessions launch the configured Claude command, Codex Sessions launch the configured Codex command, and generic TUI Sessions launch the configured shell command. Operators can override each profile with environment variables, lifecycle status reports whether the first executable token is currently resolvable, and terminal-control actors can explicitly run bounded version probes and adapter capability detection. Readiness checks deliberately do not execute agent commands; version probes and external handshake probes execute only on explicit POST/daemon/Admin actions with timeout and output limits. Claude/Codex structured events remain gated until the adapter handshake reports `agentbridge.adapter.v1` compatibility and a supported schema version. Adapter schema matrices include provider-version evidence where available; the current Claude Hook v1 matrix is tied to the official Hook reference checked on 2026-06-26 plus local `claude --version` evidence for `2.1.193 (Claude Code)`, the current Codex app-server v1 matrix is tied to the captured `codex-cli 0.141.0` provider schema bundle, and capability detection surfaces `provider_version_verification` so unknown versions are visible instead of implied verified. External adapter processes can now use the API or `agentbridge-adapter-client` to post mapped semantic events through the `agent_adapter` source only when their agent/schema match the Session, inspect the schema behavior and provider coverage matrix, create pending Interaction records from adapter approval/question/plan events, poll or block waiting for response frames, format those responses for Claude Hook stdout or a Codex action envelope, run `claude-hook` as a stdin/stdout command hook shim, generate/merge Claude Code settings with `claude-hooks-config`, bridge one Codex app-server JSON-RPC message with `codex-app-server-event`, collect a Codex app-server JSONL stream with `codex-app-server-stream`, and launch/tee a Codex app-server stdio subprocess with `codex-app-server-proxy`, including optional live JSON-RPC response injection back into child stdin, bounded child restart policies (`never`, `on-failure`, or `always`) backed by a single upstream stdin router, optional health JSONL heartbeats for external supervisors, and documented wrapper/service-manager deployment guidance that keeps the proxy in the upstream client's stdin/stdout path.
- The adapter client offline outbox is a local durability layer, not a hidden approval path: it stores only validated event submissions after transient transport/server failures, preserves write order during automatic or explicit `flush-outbox` flushes, and keeps approval/question/plan waits fail-closed until Control Plane accepts the queued request and returns a real response.
- The Terminal Agent lifecycle outbox is also a durability layer, not a lifecycle decision engine: it preserves ordered delivery of already-decided lifecycle observations across transient Control Plane failures, while terminal execution, offline protection projection, and auto-restart policy remain governed by the normal Terminal Agent and Control Plane paths. Lifecycle monitor polling proactively flushes queued lifecycle events after reconnect, so recovery does not require a new start/lost/exit observation.
- The PTY backend uses a bounded retained-output window rather than recording all terminal output. Cursor values are absolute within the session lifetime; when a reader falls behind the retained window, `read_output`/`stream_output` returns `reset=True` with the retained tail so consoles can repaint deterministically.
- `agentbridge-pty-host` is the first independent PTY host process. It keeps PTY sessions alive across API/daemon client restarts when the host process itself remains running. The host and `pty_host` client reread `AGENTBRIDGE_TERMINAL_PTY_HOST_TOKEN_FILE` per request, allowing shared token rotation without process restarts while failing closed when the token file is the only configured credential and becomes unreadable or empty. The `pty_host` client can auto-start the host and clean stale Unix sockets only when connection probing proves there is no listener; auth failures, timeouts, permission errors, and protocol errors preserve the socket and report the health error for operator review. The optional watchdog can restart a crashed host process. A host crash still destroys the owned PTY sessions, but combining the watchdog with lost-terminal auto-restart now gives an explicit restart-based recovery path from the last persisted allowlisted command. True process-preserving recovery after host death is not possible with the current stdlib PTY owner model.
- Terminal exit observation can be driven by the in-process lifecycle monitor, so clients do not have to call `status` to produce `terminal.exited`. The monitor recovers tracked terminal generations from semantic events, emits `terminal.lost` when a recovered generation has no observable backend session, and projects that loss into offline protection so Bot writes pause even after process recovery. `restart_session` can explicitly recover a lost/exited backend from the latest persisted command and exits offline protection after `terminal.started`; the opt-in auto-restart policy can do that for lost recovered terminals with a bounded attempt count after the command matches the allowlist. Service-manager deployment is now documented for the PTY host; production lifecycle supervision still needs a persistent scheduler if automatic policy execution must survive the supervising API/daemon process itself.
- The documented product-like PTY host topology is now OS service-manager first: run one `agentbridge-pty-host` per local user under systemd user services or macOS launchd, and let API/daemon clients connect over the token-gated Unix socket. In-process auto-start/watchdog remains useful as a local fallback but should not compete with an enabled service-manager unit.
- Automatic lost-terminal restart is disabled by default because restarting a native CLI can have side effects. Operators must opt in with `AGENTBRIDGE_TERMINAL_AUTO_RESTART_ON_LOST=true` and explicitly allow replayable commands with `AGENTBRIDGE_TERMINAL_AUTO_RESTART_COMMAND_ALLOWLIST`; an empty allowlist blocks automatic replay and emits `terminal.auto_restart.skipped` for operator review. `AGENTBRIDGE_TERMINAL_AUTO_RESTART_MAX_ATTEMPTS` bounds restart attempts per service process.
- Terminal lifecycle run-once is treated as an operational action because it can emit `terminal.exited`/`terminal.lost` and trigger opt-in auto-restarts. The REST endpoint requires `terminal_control` for managed device credentials and terminal-control actor permission; read-only lifecycle status still requires `audit.view` at the actor/RBAC layer and `terminal_read` for managed device credentials.
- Desktop terminal auto-open is opt-in. Custom command templates remain supported, and built-in presets cover macOS Terminal plus common Linux terminal emulators. The daemon keeps sensitive local token/socket state out of launched argv; macOS Terminal uses a mode-0700 short-lived launcher script because AppleScript cannot directly propagate the daemon's environment into the new shell.
- The tmux backend treats an existing `agentbridge_<session-id>` session as resumable state after Agent restart, matching the design's MVP recovery path.
- Rendering is split into platform-neutral documents and platform renderers. The first renderer intentionally targets text fallback so unsupported Bot platforms still receive coherent output.
- Assistant answer deltas and completion events use a stable render-document update key per Turn, while Bot Gateway frames keep per-event idempotency keys. Platform adapters can therefore incrementally edit or merge one answer stream without losing duplicate-delivery protection.
- Text fallback rendering treats fenced code blocks as structural boundaries during message splitting. If a code block exceeds the platform message length, AgentBridge rewraps each code-content slice in its own balanced fence instead of sending an unterminated code block across messages.
- Tool progress rendering summarizes `tool.started`, `tool.output.delta`, `tool.completed`, and `tool.failed` with tool name, adapter item ID, output, or error fields instead of dumping the preserved `raw_event` payload into chat.
- Bot delivery idempotency is implemented before real platform integration so duplicate event replay cannot cause duplicate sends once a real transport is attached.
- Bot delivery records are persisted separately from semantic events so replay, delivery retries, and platform message IDs can evolve without mutating event history.
- OneBot outbound delivery is implemented as a transport contract first. The NoneBot wrapper is optional and dependency-free; matcher/lifecycle registration helpers cover common single- or multi-matcher `matcher.handle()` setup paths, command-registration helpers expose the shared manifest/result protocol plus startup hook wiring to application code, `register_nonebot_lifecycle()` can keep inbound matcher wiring and startup command registration on one plugin instance, and `NoneBotTransport` can send/delete OneBot V11 text deliveries through `send_to()`/`send()` wrappers or `call_api()`. Full NoneBot integration still needs richer message components, deeper driver/bot lifecycle integration, and adapter-specific edit/rich-card capabilities.
- Delivery retry state is stored on delivery records, not events, so the immutable semantic event stream remains replayable while platform delivery can fail and recover independently.
- The retry worker reuses the Bot Gateway retry path instead of writing records directly. This keeps manual retry, background retry, and future scheduler behavior consistent.
- Platform rate-limit policies intentionally schedule unsent records as `retrying` instead of sleeping inside request handlers. This keeps API calls bounded and leaves actual waiting to the retry worker.
- Observed platform rate-limit responses are also stored as `retrying`, but keep the incremented attempt count because the platform was actually contacted.
- Interaction commands now route through the same command parser and audit chain as project/session commands. Approval voting is permission-gated by `approval.vote`; answering questions and plan revision feedback are gated by `session.send`; plan cancellation uses `session.manage`.
- Manual Control Plane interaction creation emits type-specific request events for approvals, questions, and plans (`approval.requested`, `question.requested`, and `plan.requested`) so Bot renderers and adapter response polling can use the same event vocabulary.
- Interaction expiry is a terminal state and never auto-approves. Reads and interaction actions opportunistically advance due interactions to `expired` so pending lists do not show stale approval requests.
- Approval policy snapshots are copied onto each approval interaction so later policy changes do not rewrite historical approval requirements.
- Approval quorum overrides are intentionally scoped and snapshotted at interaction creation. Chat-context overrides win over project overrides, and explicit per-interaction `required_votes` remains the strongest override.
- OneBot inbound support executes text commands and command-carrying action callbacks through the same command path. The optional NoneBot wrapper shares that callback payload parser so render action descriptors behave consistently across direct OneBot HTTP and NoneBot matcher integrations.
- OneBot reply fallback intentionally resolves Interaction IDs only through existing Bot delivery records for the same chat context and platform message ID; if the replied message cannot be tied to a delivered semantic event with an `interaction_id`, command parsing keeps the explicit-ID requirement. The fallback is limited to Interaction commands where the replied message supplies the target (`answer`, `approve`, `deny`, and plan show/approve/revise/cancel), and does not rewrite unrelated commands such as `plan list`.
- Group role bindings are scoped to a chat context and actor ID. This keeps OneBot user permissions local to the group/private context while still allowing command/API callers to carry bootstrap roles.
- Access policy rules are stored separately from approval quorum overrides. Approval policy answers "how many votes"; access policy answers "who may do which action".
- Access policy evaluation is deny-first and then allow-before-RBAC. This makes temporary freezes explicit while preserving the existing role matrix as the default baseline.
- Access policy enforcement uses stable resource attributes only: IDs, status, project/session linkage, visibility, agent type, chat context, operation, risk level, and owner metadata. It intentionally avoids volatile or sensitive filesystem path values as policy attributes.
- Managed device identity scopes are mostly coarse transport gates (`http_api`, event WebSockets, terminal WebSocket, and Bot Gateway WebSocket), not a replacement for actor RBAC or access-policy rules. `audit_read`, `bot_gateway_read`, `bot_gateway_manage`, `onebot_event_ingest`, `command_parse`, `command_execute`, `device_manage`, `policy_read`, `policy_manage`, `group_role_read`, `group_role_manage`, `chat_context_manage`, `project_read`, `project_manage`, `session_read`, `session_manage`, `session_send`, `session_event_ingest`, `interaction_read`, `interaction_manage`, `terminal_read`, and `terminal_control` are the first action-scoped managed-device permissions; they are required for managed credentials to call audit/event history HTTP read APIs, Bot Gateway HTTP read APIs, Bot Gateway HTTP mutation APIs, OneBot inbound event APIs, command parsing, command execution, device identity APIs, access/approval policy read and simulation APIs, access/approval policy write APIs, chat-context role read APIs, chat-context role write APIs, chat-context creation/pointer write APIs, project/workspace read APIs, project/workspace/binding write APIs, session, queued-Turn, and current writer-lease read APIs, session lifecycle, writer-lease write, queue clear, queue reorder, and queue pause/resume APIs, direct session turn enqueue and queued-turn removal APIs, Terminal Agent session event ingestion APIs, interaction/approval read APIs, interaction/approval write APIs, terminal-read HTTP APIs including `/api/v1/readiness`, and terminal-control HTTP APIs. `allowed_resource_ids` can additionally bind a managed device key or managed certificate fingerprint to specific project, session, interaction, chat-context, or device-identity IDs visible in REST paths, REST query parameters, bounded top-level JSON body fields, and WebSocket route parameters; empty allowlists keep the legacy all-resource behavior, `*` is an explicit wildcard, and non-empty allowlists fail closed when a request has no visible resource ID. Command execution remains a broad entrypoint whose effects are still constrained by the command actor's RBAC and access-policy decision; direct turn enqueue and OneBot inbound commands likewise still check the effective actor permissions. Managed device identities bind device keys and active client certificate fingerprints to the same scope, resource allowlist, and revocation lifecycle, while `certificate_records` retain import/rotation/CA issuance metadata and removal timestamps for audit, renewal planning, and operations. The certificate fingerprint gates assume a trusted TLS-terminating proxy has already verified mTLS and stripped spoofable inbound fingerprint headers before setting the configured header for AgentBridge.
- Bot Gateway delivery result/edit/delete authorization treats the delivery record's `chat_context_id` as the stable resource for the posted delivery `idempotency_key`; the idempotency key itself is not a first-class allowlist resource, preserving the existing "all requested resource IDs must be allowed" semantics for managed device allowlists.
- The Admin Web UI is intentionally API-backed and build-free for now. It has a small `/admin` entrypoint, system health status aggregation with `/api/v1/readiness` MVP acceptance pass/warn/fail counts, warn/fail next-step action items, and JSON export for acceptance evidence, project/session operations for project inventory, workspace registration, session creation, session closure, active Turn/queue/pending-approval/lease status, queue controls, and JSON evidence export, interaction/approval operations for listing, filtering, creating, answering, voting, cancelling, and JSON evidence export, audit/event exploration for filtered audit records plus JSON/CSV/signed-archive export and semantic event search/replay/live tail, access policy editing, terminal lifecycle inspection/run-once/event-outbox flush/probe/detect with adapter provider-version verification summaries and JSON evidence export, device identity create/rotate/revoke with redacted JSON evidence export, and Bot delivery operations for records, retry worker status, due retry, selected-delivery edit/delete mutation, platform capabilities, native command registration telemetry, rate limits, and JSON evidence export. The `/api/v1/readiness` report now covers HTTP API, Admin Web, WebSocket, device credential, and client certificate authentication gates, warning on missing product gates and failing configured-but-unusable token files, static device-key JSON, managed-device credentials, or certificate fingerprint sources. It can also read `AGENTBRIDGE_ACCEPTANCE_EVIDENCE_FILE` as an `agentbridge.acceptance_evidence.v1` manifest, warning when no manual MVP sign-off manifest is configured, treating unreadable/malformed manifests or manifests with unknown section IDs as failures, flagging failed or malformed design sections, and requiring passed status, passed manual checklist items, and safe non-duplicated artifact references for design-document sections 34.1 through 34.8 before acceptance checks go green. Readiness fails sections with non-object section values, non-list checklist fields, non-list artifact fields, unsafe artifact paths, duplicate artifact paths, or malformed sha256 digests; with `AGENTBRIDGE_ACCEPTANCE_VERIFY_ARTIFACTS=true`, it also verifies those artifact files under `AGENTBRIDGE_ACCEPTANCE_ARTIFACT_ROOT` or the manifest directory and fails sections with missing files, root escapes, non-files, or sha256 mismatches. `AGENTBRIDGE_ACCEPTANCE_BUNDLE_FILE` adds a release-candidate bundle gate to the same readiness report: missing bundles warn, invalid bundles fail, verified draft bundles warn, ready verified bundles pass, includes artifact/checklist summary counts in the bundle check evidence, and a bundle whose manifest hash differs from the configured evidence manifest fails. `agentbridge-acceptance` initializes, updates, summarizes, lists incomplete checklist items with `summary --show-checklist`, records per-section manual checklist item status, attaches copied release-candidate evidence files into the artifact root with computed sha256 manifest entries, validates and attaches supported Admin JSON exports under stable section-scoped names with page-specific top-level evidence field checks plus System Health readiness action evidence-summary checks, rejects Admin exports attached outside their recommended design-document sections unless explicitly overridden, bundles ready evidence into a portable ZIP containing the manifest, bundle index, and verified artifact files without embedding the local artifact root path, and verifies those ZIPs offline without extraction while rejecting unindexed ZIP entries, non-object bundled section values, checking indexed artifact section ownership, requiring unique indexed artifact paths and archive paths, and recomputing and checking artifact/checklist summary counts from the bundled manifest; optional `PATH=SHA256` artifact entries remain available for preexisting files, so operators can record section-level sign-off without hand-editing JSON. The `agentbridge-release` CLI now provides a release-candidate preflight over the local handoff boundary, checking package version metadata, console scripts, required runbooks/templates, product-like database/auth/terminal configuration, and configured acceptance manifest/bundle files before a first usable handoff. The `agentbridge-readiness` CLI reads the same protected readiness report with API-token or managed-device credentials, supports JSON, summary, and operator-action output that includes acceptance manifest, section, and bundle artifact/checklist summary counts for non-passing acceptance checks, and can return non-zero for degraded/not-ready deployments through `--fail-on-warn` or `--fail-on-fail` so deployment scripts can enforce acceptance gates; `docs/operations/MVP_ACCEPTANCE_RUNBOOK.md` maps that automated gate to the design document's manual MVP acceptance evidence, and `docs/operations/RELEASE_CANDIDATE.md` defines the first usable RC handoff flow. `AGENTBRIDGE_ADMIN_TOKEN(_FILE)` gates the built-in browser pages with token unlock plus an HttpOnly/SameSite cookie while preserving zero-config local development when unset. The optional REST API token gate accepts that unlocked admin cookie so browser-admin API calls are covered by the same gate, protected WebSocket event streams accept the cookie for same-origin Admin pages, and `AGENTBRIDGE_API_TOKEN(_FILE)` can serve as the admin unlock token when no separate admin token is configured. Richer dashboards remain future work.
- Session creation is evaluated against the target project because the session resource ID does not exist yet. Terminal control is evaluated as `resource_type=terminal` with the session ID so terminal-specific rules do not have to overmatch ordinary session sends.
- WebSocket session streams, Terminal command WebSocket replay, and local daemon semantic-event replay are read-side transports over immutable semantic events. They use `after_seq` cursors for replay/reconnect and do not mutate Bot delivery records; Terminal command WebSocket and local daemon consumers can additionally ACK processed session events so reconnects can resume from Control Plane's saved per-consumer cursor.
- Bot Gateway WebSocket subscriptions fan out render frames for external platform adapters, but do not store delivery records. Platform adapters report delivery acknowledgements, edits, and deletes explicitly through the delivery-result API keyed by message idempotency key.
- Bot Gateway session-event WebSocket replay uses per-stream `after_seq`, matching session semantic event streams. Bot Gateway notification WebSocket replay also accepts `after_event_id` because chat-context notifications can interleave system, project, and session streams whose `seq` values are only stream-local.
- Bot Gateway HTTP delivery can target session streams or filtered cross-stream semantic events. Cross-stream delivery requires at least one event filter so an operator cannot accidentally fan out unrelated recent events to a chat context. The certificate scan worker can automatically deliver the latest action-required scan event to configured chat contexts through the same delivery path.
- Cross-stream Bot Gateway delivery emits one idempotent `bot.notification` semantic event per source event, chat context, and platform, allowing system/project/session notifications to be consumed through the Bot Gateway notification WebSocket without creating delivery records in the subscriber path.
- Bot delivery platform state is stored on delivery records, not semantic events. Edits/deletes update platform lifecycle metadata and latest delivery text without rewriting the immutable event stream.
- Bot Gateway edit/delete APIs call transport-native operations first, then record platform state and emit the corresponding `bot.render.update` or `bot.render.delete` protocol event. OneBot V11 supports deletion through `delete_msg`; message editing remains platform-specific, but deployments can enable a non-standard OneBot edit action with `AGENTBRIDGE_ONEBOT_EDIT_ACTION`, in which case capability reporting exposes `editMessage=true` and edit calls post `message_id` plus the configured text field.
- Render actions are emitted twice: as plain-text fallback commands and as structured button, select, or modal descriptors in Bot Gateway WebSocket frames. Platform adapters own conversion into native buttons/cards/select menus/modals and can send callbacks carrying the descriptor payload command, or select/modal payloads carrying `command_template` plus submitted values, back to `POST /api/v1/onebot/events` or the NoneBot wrapper; AgentBridge still re-authorizes the clicking/submitting `user_id` through the normal command actor path. `bot.interaction.ack` is emitted only after the command succeeds, reuses platform event idempotency to keep callback retries from producing duplicate acknowledgements, and is forwarded as a same-type Bot Gateway WebSocket frame instead of a generic render-create frame.
- Bot Gateway upstream events are recorded separately from command execution results. Ignored plain messages are still captured as `bot.message.received`, while executable messages/actions/submissions also pass through the existing command parser, RBAC, idempotency, audit, and ack paths.
- The platform-neutral upstream endpoint is adapter-facing and conservative: command, slash-command, action, selection, and modal events execute only when command text is present or explicitly requested; plain message and attachment events are recorded without becoming tasks by default.
- Native command-registration results are adapter telemetry, not command execution. They are stored as `bot.command_registration.result` semantic events so operators can inspect platform menu drift/failures through the same event search and delivery surfaces without adding mutable delivery state.
- The command registry is intentionally scoped to implemented commands. Design-document commands that are not executable yet stay out of the manifest so platform adapters do not advertise broken native menu entries.
- Bot Gateway capability reporting is conservative and platform-standard oriented: WebSocket action descriptors remain adapter hints, while `GET /api/v1/bot-gateway/capabilities` reports OneBot V11 native button/select/modal support as unavailable unless a future platform-specific transport owns that capability; OneBot `editMessage` is reported only when the configured transport exposes an edit extension.
- Text fallback Interaction replies reuse the same delivery/event chain: a reply to a Bot-delivered question, approval, or plan can omit the Interaction ID for supported interaction commands, while the resolved command still runs as the replying OneBot actor and passes normal RBAC/access-policy checks.
- `AGENTBRIDGE_API_TOKEN(_FILE)`, `AGENTBRIDGE_WS_TOKEN(_FILE)`, `AGENTBRIDGE_ADMIN_TOKEN(_FILE)`, `AGENTBRIDGE_DEVICE_KEYS`, persisted device identities, and trusted-proxy `AGENTBRIDGE_CLIENT_CERT_FINGERPRINTS(_FILE)` are the current gates for HTTP API clients, WebSocket clients, built-in admin pages, and per-device/client-certificate clients. Token and global fingerprint files are reread per request/connection and never disable a configured gate when unreadable or empty; if no other source exists, auth fails closed. Browser Admin sessions use an HttpOnly cookie for REST calls and same-origin event WebSocket streams so the unlock token does not need to be readable from JavaScript. Managed device keys are stored as salted PBKDF2 hashes, existing keys are preserved unless explicitly rotated with a non-empty `device_key`, active managed certificate fingerprints are normalized before persistence, CA-issued certificates persist serial/subject/issuer/validity metadata, renewals retire old active CA-issued fingerprints with removal metadata, add/remove rotation retains removal timestamps, and `certificate_health` reports expiring/expired/unknown certificate inventory plus renewal scheduled/due/overdue/unknown status using `AGENTBRIDGE_DEVICE_CERT_EXPIRY_WARNING_DAYS` (default 14) as the planning window. `AGENTBRIDGE_DEVICE_CERT_ISSUER_COMMAND` lets deployments delegate device certificate issuance to an external CA/KMS/HSM command; AgentBridge validates the CSR before calling it and validates the returned certificate CN, public key, expiry, clientAuth EKU, CA BasicConstraints, and direct CA signing relationship before persisting. `device_identity.certificates_scanned` is the current notification event for manual or worker-driven expiry and renewal scans, renders for operators through `GET /api/v1/events/rendered`, can be delivered to Bot chat contexts through filtered `POST /api/v1/bot-gateway/deliver-events`, and can be routed automatically by the scan worker when `AGENTBRIDGE_DEVICE_CERT_SCAN_NOTIFY_CHAT_CONTEXT_IDS` is configured; `AGENTBRIDGE_DEVICE_CERT_SCAN_WORKER_ENABLED` enables periodic scans. `docs/operations/DEVICE_CERTIFICATE_OPERATIONS.md` covers the renewal schedule/cutover runbook and current TLS proxy/CA custody boundary. Known expired managed certificate records are not accepted for fingerprint authentication; imported fingerprints without validity metadata remain allowed but are reported as unknown health. Certificate-only identities do not receive an implicit fallback key, and optional `allowed_resource_ids` constrain managed credentials to visible REST/WebSocket resource IDs including supported top-level JSON body fields bounded by `AGENTBRIDGE_DEVICE_AUTH_RESOURCE_BODY_LIMIT_BYTES` (default `1048576`). Managed device gates fail closed once any managed identity exists, even if all managed identities are revoked. These gates still need provider-native CA SDK clients, managed verifier tooling, and deeper production TLS/key lifecycle enforcement.
- The original design document remains unchanged; this file is the rolling handoff/progress document for future sessions.

## Verification

Run:

```bash
uv sync --extra dev
uv run pytest
uv run ruff check .
uv run python -m compileall -q src tests alembic
AGENTBRIDGE_DATABASE_URL=sqlite:////tmp/agentbridge-check.db uv run alembic upgrade head
uv run agentbridge-release --profile local --format summary
```

## Next Development Backlog

1. Complete native Claude Hook and Codex app-server adapters behind the adapter capability/event ingress/response contract by adding broader verified provider-version behavior captures beyond the first Claude Hook and Codex app-server v1 provider projections.
2. Harden PTY host recovery beyond watchdog plus command restart, including cross-platform socket/pipe cleanup and Windows ConPTY/Named Pipe parity.
3. Expand the Admin Web UI beyond readiness/system health, project/session operations, queue controls, interaction/approval operations, audit/event live/search exploration, access policy, terminal lifecycle, device identity management, and Bot delivery operations, including additional live dashboards and normalized payload search.
4. Extend the HTTP API/WebSocket/admin/device-key/client-cert-fingerprint gates beyond the current readiness-visible credential checks with provider-native CA SDK clients, managed verifier tooling, native production TLS/key lifecycle enforcement, and deeper command/payload-aware or method-specific resource/action device authorization.
5. Add platform-specific rich card/button transport adapters and outbound edit extensions.
6. Add deeper native NoneBot lifecycle hooks and richer message-component integration once a stronger dependency boundary is selected.
